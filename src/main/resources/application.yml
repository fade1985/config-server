server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/fade1985/config-server
          clone-on-start: true
security:
  user:
    name: root
    password: s3cr3t